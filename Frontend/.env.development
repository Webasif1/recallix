# Local dev: the Vite server (5173) talks to the backend dev server (3000).
# Leave VITE_API_URL EMPTY/unset in production — the built client is served by
# the same Express process that serves the API, so relative "/api/..." is right.
VITE_API_URL=http://localhost:3000
