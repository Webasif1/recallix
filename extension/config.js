// Where the extension sends saves.
//
// Point this at your own deployment (and update host_permissions in
// manifest.json to match) — switch to http://localhost:3000 for local work.
export const API_BASE = "https://recallix.onrender.com";
